<p id="notice"><%= notice %></p>

<p>
  <strong>User:</strong>
  <%= @asset.project_id %>
</p>

<%= link_to 'Edit', edit_asset_path(@project.id, @asset) %> |
<%= link_to 'Back', project_assets_path %>
